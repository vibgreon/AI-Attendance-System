I've implemented it for a web application, though it is working
very slow compared to the actual program.

I think the reason for being slow is that I'm using the library instead of a pre-trained model
that calls each frame and implements a face recognition algorithm from scratch every time.